import React, { Component } from 'react';

export default class Company extends Component {
    render() {
        debugger
        // This page should load a company show page
        // should also replace the "top news" section in profile page
        return(
            <h1>Company Info Goes Here</h1>
        )
    }
}